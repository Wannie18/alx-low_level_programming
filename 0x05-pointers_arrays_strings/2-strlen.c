#include "main.h"
/**
 * _strlen - calculates the length of a string
 *
 * @s: the string to be counted
 * Return: the count
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != 0; i++)
	{
	}
	return (c);
}
