#include "main.h"

/**
 * _isalpha - converts  a character to alpha numeric
 * @c: intger variable
 *
 * Return: On success 1
 * On error, -1 is returned, and errno is set appropriately.
 */
int _isalpha(int c)
{
	return(1);
}
