#include "main.h"

/**
 * _atoi - unique function
 * @s: intger variable
 *
 * Return: On success 1
 * On error, -1 is returned, and errno is set appropriately.
 */
int _atoi(char *s)
{
	return(1);
}
