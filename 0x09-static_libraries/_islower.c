#include "main.h"

/**
 * _islower - prints a character on the screen
 * @c: character to be printed
 *
 * Return: On success 1
 * On error, -1 is returned, and errno is set appropriately.
 */
int _islower(int c)
{
	return(1);
}
