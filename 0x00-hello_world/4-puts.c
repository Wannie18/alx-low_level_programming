#include<stdio.h>
/**
 * This the main function where execution starts
 */
int main(void)
{
	char PrintStr[] = "\"Programming is like building a multilingual puzzle";

	puts(PrintStr);
	return (0);
}
